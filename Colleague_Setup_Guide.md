# 下水道科管理系統 - 同事使用指南 (Windows 單機版)

這是一份給「下水道科」同仁的系統開啟指南。您可以將整個資料夾複製到您的電腦中使用。

## 準備工作 (僅需執行一次)

1. **安裝 Node.js**:
   - 您的電腦需要安裝 Node.js 才能執行本系統。
   - 請至 [Node.js 官網](https://nodejs.org/zh-tw) 下載 **LTS 版本** (穩定版) 並安裝。

## 如何開啟系統

1. **進入系統資料夾**:
   - 打開您收到的 `sewerage-system` 資料夾。

2. **點擊啟動檔案**:
   - 找到名為 `start-win.bat` 的檔案。
   - **雙擊執行** 它。

3. **開始使用**:
   - 黑色視窗會跳出並顯示啟動進度。
   - 啟動完成後，您的預設瀏覽器會自動開啟至 `http://localhost:3000`。
   - **請勿關閉** 啟動後的黑色命令提示字元視窗，那是系統的後台程式。

## 注意事項

- **資料庫保存**: 系統的所有資料都存放在 `sewerage.db` 檔案中。若您要將資料傳給其他同事，請務必連同此檔案一起傳送。
- **路徑問題**: 建議將系統資料夾放在路徑沒有中文字或空格的地方 (例如 `D:\SewerSystem`)，雖然大部分情況下中文路徑也能執行。
- **街景圖功能**: 瀏覽器必須能夠連上網際網路才能正確顯示 Google 街景圖。

---
*本系統由 AI 協作開發，旨在提升下水道科業務效率。*
